name: Build deb

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build dylib
        run: |
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          clang -arch arm64 \
                -isysroot "$SDK" \
                -dynamiclib -fobjc-arc \
                -Wno-deprecated-declarations \
                -install_name @executable_path/Frameworks/FullScreenBackTweak.dylib \
                -framework UIKit \
                -framework Foundation \
                -framework AVFoundation \
                -o FullScreenBackTweak.dylib \
                FullScreenBackTweak.m
          ls -la FullScreenBackTweak.dylib
      - uses: actions/upload-artifact@v4
        with:
          name: dylib
          path: |
            FullScreenBackTweak.dylib
            FullScreenBackTweak.plist
            PreferenceBundles/FullScreenBackTweak.bundle/Root.plist
            FullScreenBackTweak_Prefs.plist

  package:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dylib
      - name: Package deb
        run: |
          mkdir -p deb/DEBIAN
          mkdir -p deb/var/jb/Library/MobileSubstrate/DynamicLibraries
          mkdir -p deb/var/jb/Library/PreferenceBundles
          
          printf "Package: com.fullscreenback.tweak\nName: FullScreenBackTweak\nVersion: 1.0.0\nArchitecture: iphoneos-arm64\nDescription: Full Screen Back Tweak\nMaintainer: custom\nAuthor: custom\nSection: Tweaks\nDepends: firmware (>= 15.0), preferenceloader\nPriority: optional\n" > deb/DEBIAN/control
          
          cp FullScreenBackTweak.dylib deb/var/jb/Library/MobileSubstrate/DynamicLibraries/
          cp FullScreenBackTweak.plist deb/var/jb/Library/MobileSubstrate/DynamicLibraries/
          cp FullScreenBackTweak_Prefs.plist deb/var/jb/Library/MobileSubstrate/DynamicLibraries/
          cp -r PreferenceBundles/FullScreenBackTweak.bundle deb/var/jb/Library/PreferenceBundles/
          
          dpkg-deb --build --root-owner-group deb FullScreenBackTweak.deb
          ls -la FullScreenBackTweak.deb
      - uses: actions/upload-artifact@v4
        with:
          name: FullScreenBackTweak.deb
          path: FullScreenBackTweak.deb